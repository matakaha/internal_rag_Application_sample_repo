name: Deploy to Azure Web Apps

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: 'aci-runner-deployment'
  cancel-in-progress: false

env:
  RESOURCE_GROUP: 'rg-internal-rag-dev'
  WEBAPP_NAME: 'app-internal-rag-dev'
  VNET_NAME: 'vnet-internal-rag-dev'
  SUBNET_NAME: 'snet-container-instances'
  LOCATION: 'japaneast'
  AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
  AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
  AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

jobs:
  setup-runner:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    outputs:
      runner-name: ${{ steps.setup-runner.outputs.runner-name }}
      container-name: ${{ steps.setup-runner.outputs.container-name }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Setup Container Instance Runner
        id: setup-runner
        run: |
          # 動的なコンテナ名とRunner名を生成
          CONTAINER_GROUP_NAME="aci-runner-${{ github.run_id }}"
          RUNNER_NAME="runner-${{ github.run_id }}"
          echo "runner-name=$RUNNER_NAME" >> $GITHUB_OUTPUT
          echo "container-name=$CONTAINER_GROUP_NAME" >> $GITHUB_OUTPUT

          # GitHub Runner登録トークン取得
          RUNNER_TOKEN=$(curl -s -X POST \
            -H "Authorization: token ${{ secrets.GH_PAT }}" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${{ github.repository }}/actions/runners/registration-token" \
            | jq -r .token)

          echo "RUNNER_TOKEN取得完了"

          # ACRのリソースIDを取得
          ACR_ID=$(az acr show --name acrinternalragdev --resource-group $RESOURCE_GROUP --query id -o tsv)
          
          # User Assigned Managed Identityを作成
          IDENTITY_NAME="id-aci-runner-${GITHUB_RUN_ID}"
          echo "Creating User Assigned Managed Identity..."
          az identity create \
            --name $IDENTITY_NAME \
            --resource-group $RESOURCE_GROUP \
            --location $LOCATION
          
          # IdentityのIDを取得
          IDENTITY_ID=$(az identity show \
            --name $IDENTITY_NAME \
            --resource-group $RESOURCE_GROUP \
            --query id -o tsv)
          
          IDENTITY_PRINCIPAL_ID=$(az identity show \
            --name $IDENTITY_NAME \
            --resource-group $RESOURCE_GROUP \
            --query principalId -o tsv)
          
          echo "Identity Principal ID: $IDENTITY_PRINCIPAL_ID"
          
          # Managed IdentityがEntra IDに伝播するまで待機
          echo "Waiting for Managed Identity to propagate to Entra ID..."
          sleep 60
          
          # IdentityにACR Pull権限を付与
          echo "Assigning AcrPull role to User Assigned Identity..."
          az role assignment create \
            --assignee $IDENTITY_PRINCIPAL_ID \
            --role AcrPull \
            --scope $ACR_ID
          
          # 権限の伝播を待つ
          echo "Waiting for role assignment to propagate..."
          sleep 30
          
          # User Assigned Managed Identityを使ってコンテナ作成
          # Private Endpoint経由でACRにアクセス(vNet内部通信)
          echo "Creating GitHub Runner container: $CONTAINER_GROUP_NAME"
          az container create \
            --resource-group $RESOURCE_GROUP \
            --name $CONTAINER_GROUP_NAME \
            --image acrinternalragdev.azurecr.io/github-runner:latest \
            --assign-identity $IDENTITY_ID \
            --acr-identity $IDENTITY_ID \
            --os-type Linux \
            --vnet $VNET_NAME \
            --subnet $SUBNET_NAME \
            --location $LOCATION \
            --cpu 2 \
            --memory 4 \
            --restart-policy Never \
            --secure-environment-variables \
              RUNNER_TOKEN=$RUNNER_TOKEN \
              GITHUB_TOKEN=${{ secrets.GH_PAT }} \
            --environment-variables \
              GITHUB_REPOSITORY=${{ github.repository }} \
              RUNNER_REPOSITORY_URL=https://github.com/${{ github.repository }} \
              RUNNER_NAME=$RUNNER_NAME \
              RUNNER_LABELS=self-hosted,azure,vnet \
              RUNNER_WORK_DIRECTORY=_work
          
          echo "Container created. Checking initial status..."
          
          # 初期状態を確認
          sleep 10
          az container show \
            --resource-group $RESOURCE_GROUP \
            --name $CONTAINER_GROUP_NAME \
            --query "{state:instanceView.state, containers:containers[0].instanceView.currentState}" \
            -o json

          # Runnerが登録されるまで待機（デバッグ情報付き）
          echo "Waiting for runner to register with GitHub..."
          MAX_WAIT=300
          ELAPSED=0
          
          while [ $ELAPSED -lt $MAX_WAIT ]; do
            # コンテナの状態を確認
            CONTAINER_STATE=$(az container show \
              --resource-group $RESOURCE_GROUP \
              --name $CONTAINER_GROUP_NAME \
              --query "instanceView.state" -o tsv 2>/dev/null || echo "Unknown")
            
            # コンテナが失敗した場合、ログを出力して終了
            if [ "$CONTAINER_STATE" = "Failed" ] || [ "$CONTAINER_STATE" = "Terminated" ]; then
              echo "❌ Container is in $CONTAINER_STATE state!"
              echo "Container logs:"
              az container logs \
                --resource-group $RESOURCE_GROUP \
                --name $CONTAINER_GROUP_NAME || echo "Unable to retrieve logs"
              
              echo "Container events:"
              az container show \
                --resource-group $RESOURCE_GROUP \
                --name $CONTAINER_GROUP_NAME \
                --query "containers[0].instanceView.events[]" -o table
              
              exit 1
            fi
            
            # Runnerのステータス確認
            RUNNER_STATUS=$(curl -s \
              -H "Authorization: token ${{ secrets.GH_PAT }}" \
              -H "Accept: application/vnd.github+json" \
              "https://api.github.com/repos/${{ github.repository }}/actions/runners" \
              | jq -r ".runners[] | select(.name==\"$RUNNER_NAME\") | .status")
            
            if [ "$RUNNER_STATUS" = "online" ]; then
              echo "✅ Runner is online and ready!"
              break
            fi
            
            echo "Container: $CONTAINER_STATE | Runner: ${RUNNER_STATUS:-not found} | Waiting... ($ELAPSED/$MAX_WAIT seconds)"
            sleep 10
            ELAPSED=$((ELAPSED + 10))
          done
          
          if [ $ELAPSED -ge $MAX_WAIT ]; then
            echo "❌ Error: Runner did not come online within $MAX_WAIT seconds"
            echo "Final container logs:"
            az container logs \
              --resource-group $RESOURCE_GROUP \
              --name $CONTAINER_GROUP_NAME || echo "Unable to retrieve logs"
            exit 1
          fi

  build-and-push-image:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    outputs:
      image-name: ${{ steps.docker-build.outputs.image-name }}
      image-tag: ${{ steps.docker-build.outputs.image-tag }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Build and Push Docker Image to ACR
        id: docker-build
        run: |
          echo "=== Building Docker Image using ACR Build ==="
          
          ACR_NAME="acrinternalragdev"
          IMAGE_NAME="webapp-frontend"
          IMAGE_TAG="$(date +%s)"
          
          echo "Building image: ${IMAGE_NAME}:${IMAGE_TAG}"
          
          # ACR上でビルド&プッシュ (ローカルDocker不要)
          az acr build \
            --registry $ACR_NAME \
            --image ${IMAGE_NAME}:${IMAGE_TAG} \
            --image ${IMAGE_NAME}:latest \
            --file Dockerfile \
            .
          
          echo "✅ Image built and pushed successfully"
          echo "image-name=${IMAGE_NAME}" >> $GITHUB_OUTPUT
          echo "image-tag=${IMAGE_TAG}" >> $GITHUB_OUTPUT

  deploy-webapp:
    needs: [setup-runner, build-and-push-image]
    runs-on: self-hosted
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure Web App for Container Deployment
        run: |
          echo "=== Configuring Web App for Container ==="
          
          ACR_NAME="acrinternalragdev"
          IMAGE_NAME="${{ needs.build-and-push-image.outputs.image-name }}"
          IMAGE_TAG="${{ needs.build-and-push-image.outputs.image-tag }}"
          
          # Web App の Container 設定を更新
          az webapp config container set \
            --resource-group ${{ env.RESOURCE_GROUP }} \
            --name ${{ env.WEBAPP_NAME }} \
            --docker-custom-image-name "${ACR_NAME}.azurecr.io/${IMAGE_NAME}:${IMAGE_TAG}" \
            --docker-registry-server-url "https://${ACR_NAME}.azurecr.io"
          
          # 環境変数を設定
          az webapp config appsettings set \
            --resource-group ${{ env.RESOURCE_GROUP }} \
            --name ${{ env.WEBAPP_NAME }} \
            --settings \
              AZURE_OPENAI_ENDPOINT="${{ secrets.AZURE_OPENAI_ENDPOINT }}" \
              AZURE_OPENAI_DEPLOYMENT="${{ secrets.AZURE_OPENAI_DEPLOYMENT }}" \
              AZURE_SEARCH_ENDPOINT="${{ secrets.AZURE_SEARCH_ENDPOINT }}" \
              AZURE_SEARCH_INDEX="${{ secrets.AZURE_SEARCH_INDEX }}" \
              WEBSITES_PORT=8000
          
          echo "✅ Web App configured for container deployment"

      - name: Azure Login (on self-hosted runner)
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Restart Web App
        run: |
          echo "Restarting Web App to pull new container image..."
          
          az webapp restart \
            --resource-group ${{ env.RESOURCE_GROUP }} \
            --name ${{ env.WEBAPP_NAME }}
          
          echo "Waiting for application to start..."
          sleep 30
          
          # Web Appの状態を確認
          az webapp show \
            --resource-group ${{ env.RESOURCE_GROUP }} \
            --name ${{ env.WEBAPP_NAME }} \
            --query "{name:name, state:state, defaultHostName:defaultHostName}" \
            -o json
          
          echo "✅ Container deployment completed"

  cleanup:
    needs: [setup-runner, build-and-push-image, deploy-webapp]
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    if: always()
    steps:
      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Remove Runner from GitHub
        continue-on-error: true
        run: |
          # Runner一覧取得(GitHub PATはSecretから直接取得)
          RUNNERS=$(curl -s \
            -H "Authorization: token ${{ secrets.GH_PAT }}" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${{ github.repository }}/actions/runners")

          # 該当Runnerを削除
          RUNNER_ID=$(echo $RUNNERS | jq -r ".runners[] | select(.name==\"${{ needs.setup-runner.outputs.runner-name }}\") | .id")
          
          if [ ! -z "$RUNNER_ID" ]; then
            curl -X DELETE \
              -H "Authorization: token ${{ secrets.GH_PAT }}" \
              -H "Accept: application/vnd.github+json" \
              "https://api.github.com/repos/${{ github.repository }}/actions/runners/$RUNNER_ID"
          fi

      - name: Cleanup Resources
        run: |
          CONTAINER_GROUP_NAME="${{ needs.setup-runner.outputs.container-name }}"
          
          echo "Deleting container instance: $CONTAINER_GROUP_NAME"
          az container delete \
            --resource-group $RESOURCE_GROUP \
            --name $CONTAINER_GROUP_NAME \
            --yes || true
          
          # Managed Identityを削除
          IDENTITY_NAME="id-aci-runner-${GITHUB_RUN_ID}"
          echo "Deleting User Assigned Managed Identity: $IDENTITY_NAME"
          az identity delete \
            --name $IDENTITY_NAME \
            --resource-group $RESOURCE_GROUP || true
          
          # 7日以上前の古いACIをクリーンアップ
          echo "Cleaning up old container instances (>7 days)..."
          CUTOFF_DATE=$(date -u -d '7 days ago' '+%Y-%m-%dT%H:%M:%SZ' 2>/dev/null || date -u -v-7d '+%Y-%m-%dT%H:%M:%SZ')
          
          OLD_CONTAINERS=$(az container list \
            --resource-group $RESOURCE_GROUP \
            --query "[?starts_with(name, 'aci-runner-') && properties.instanceView.state != 'Running'].{name:name, created:properties.instanceView.events[0].lastTimestamp}" \
            -o json | jq -r ".[] | select(.created < \"$CUTOFF_DATE\") | .name")
          
          if [ -n "$OLD_CONTAINERS" ]; then
            echo "Found old containers to delete:"
            echo "$OLD_CONTAINERS"
            for container in $OLD_CONTAINERS; do
              echo "Deleting $container..."
              az container delete --resource-group $RESOURCE_GROUP --name $container --yes || true
            done
          else
            echo "No old containers to clean up"
          fi
